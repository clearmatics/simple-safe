[project]
name = "safe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click>=8.2.0",
    "safe-eth-py>=7.3.0",
]

[tool.pyright]
